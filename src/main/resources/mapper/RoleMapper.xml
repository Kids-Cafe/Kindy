<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kidscafe.kindy.mapper.IRoleMapper">
    <select id="getList" resultType="RoleDTO">
        SELECT ID, NAME
        FROM T_ROLE
        WHERE KINDERGARTEN_ID = #{kindergartenId}
    </select>

    <select id="getInfo" resultType="RoleDTO">
        SELECT ID, KINDERGARTEN_ID, NAME
        FROM T_ROLE
        WHERE ID = #{id}
    </select>

    <insert id="insert">
        INSERT INTO T_ROLE(KINDERGARTEN_ID, NAME)
        VALUES (#{kindergartenId}, #{name})
    </insert>

    <update id="updateName">
        UPDATE T_ROLE
        SET NAME = #{name}
        WHERE ID = #{id}
    </update>

    <delete id="delete">
        DELETE FROM T_ROLE
        WHERE ID = #{id}
    </delete>
</mapper>