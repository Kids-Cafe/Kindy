<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="IChatMapper">
    <select id="selectList" resultType="ChatDTO">
        SELECT ID, KINDERGARTEN_ID, CLIENT, HOST, CREATED_AT
        FROM T_CHAT
        WHERE KINDERGARTEN_ID = #{kindergartenId} OR #{kindergartenId} IS NULL
    </select>

    <select id="select" resultType="ChatDTO">
        SELECT ID, KINDERGARTEN_ID, CLIENT, HOST, CREATED_AT
        FROM T_CHAT
        WHERE KINDERGARTEN_ID = #{kindergartenId} OR #{kindergartenId} IS NULL
    </select>

    <insert id="insert">
        INSERT INTO T_CHAT(KINDERGARTEN_ID, CLIENT, HOST)
        VALUES (#{kindergartenId}, #{client}, #{host})
    </insert>

    <delete id="delete">
        DELETE FROM T_CHAT
        WHERE ID = #{id}
    </delete>
</mapper>