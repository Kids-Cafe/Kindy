<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kidscafe.kindy.mapper.IKindergartenMapper">
    <!-- `q` filters by name or address so signup search doesn't have to pull the whole table. -->
    <select id="getList" resultType="KindergartenDTO">
        SELECT ID, NAME, ADDRESS, ADDRESS_DETAIL, POSTCODE
        FROM T_KINDERGARTEN
        <if test="q != null">
            WHERE NAME LIKE CONCAT('%', #{q}, '%')
               OR ADDRESS LIKE CONCAT('%', #{q}, '%')
        </if>
        ORDER BY NAME
        LIMIT 100
    </select>

    <select id="getInfo" resultType="KindergartenDTO">
        SELECT ID, NAME, BRN, OWNER, ADDRESS, ADDRESS_DETAIL, POSTCODE, UNIX_TIMESTAMP(CREATED_AT) * 1000 CREATED_AT, UNIX_TIMESTAMP(UPDATED_AT) * 1000 UPDATED_AT
        FROM T_KINDERGARTEN
        WHERE ID = #{id}
           OR BRN = #{brn}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO T_KINDERGARTEN(NAME, BRN, OWNER, ADDRESS, ADDRESS_DETAIL, POSTCODE)
        VALUES (#{name}, #{brn}, #{owner}, #{address}, #{addressDetail}, #{postcode})
    </insert>

    <update id="update">
        UPDATE T_KINDERGARTEN
        SET NAME = #{name}, ADDRESS = #{address}, ADDRESS_DETAIL = #{addressDetail}, POSTCODE = #{postcode}
        WHERE ID = #{id}
    </update>

    <update id="updateOwner">
        UPDATE T_KINDERGARTEN
        SET OWNER = #{owner}
        WHERE ID = #{id}
    </update>

    <delete id="delete">
        DELETE FROM T_KINDERGARTEN
        WHERE ID = #{id}
    </delete>
</mapper>