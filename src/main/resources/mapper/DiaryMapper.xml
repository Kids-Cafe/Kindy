<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="IDiaryMapper">
    <select id="selectList" resultType="DiaryDTO">
        SELECT USER_ID, DATE, CONTENT, TAG, CREATED_AT
        FROM T_DIARY
        WHERE USER_ID = #{userId}
    </select>

    <select id="select" resultType="DiaryDTO">
        SELECT USER_ID, DATE, CONTENT, TAG, CREATED_AT
        FROM T_DIARY
        WHERE USER_ID = #{userId} AND DATE = #{date}
    </select>

    <insert id="insert">
        INSERT INTO T_DIARY(USER_ID, DATE, CONTENT, TAG)
        VALUES (#{userId}, #{date}, #{content}, #{tag})
    </insert>

    <update id="update">
        UPDATE T_DIARY
        SET CONTENT = #{content}, TAG = #{tag}
        WHERE USER_ID = #{userId} AND DATE = #{date}
    </update>

    <delete id="delete">
        DELETE FROM T_DIARY
        WHERE USER_ID = #{userId} AND DATE = #{date}
    </delete>
</mapper>